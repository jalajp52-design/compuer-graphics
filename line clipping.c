#include <stdio.h> #include <conio.h> #include <graphics.h> int xmin = 100, ymin = 100, xmax = 400, ymax = 400; int computeCode(int x, int y) { int code = 0; if (y > ymax) { code |= 8; } // Top if (y < ymin) { code |= 4; } // Bottom if (x > xmax) { code |= 2; } // Right if (x < xmin) { code |= 1; } // Left return code; } void main() { int gdriver = DETECT, gmode; int x1, y1, x2, y2; int code1, code2; int m; initgraph(&gdriver, &gmode, "C:\\TC\\BGI"); rectangle(xmin, ymin, xmax, ymax); printf("Enter X1 Y1: "); scanf("%d %d", &x1, &y1); printf("Enter X2 Y2: "); scanf("%d %d", &x2, &y2); m=y2-y1/x2-x1; y=m(xmax-x1)+y1; print("%d",m); print("%d",y); code1 = computeCode(x1, y1); code2 = computeCode(x2, y2); if (code1 == 0 && code2 == 0) { line(x1, y1, x2, y2); printf("Line is completely inside\n"); } else if (code1 & code2) { printf("Line is completely outside\n"); } else { line(x1, y1, x2, y2); printf("Line is partially inside\n"); } getch(); closegraph(); } calculate intercept and calculate x y and partially inside only inside part show
